{
  "name": "brvtal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:contracts": "php tests/api-contract.php && php tests/totp-enrollment-contract.php && php tests/media-library-contract.php && php tests/releases-contract.php && php tests/deployment-traceability-contract.php && php tests/public-seo-delivery-contract.php && php tests/public-entity-pages-contract.php",
    "test:integration": "php tests/integration/content-persistence.php",
    "test:e2e": "playwright test",
    "test": "npm run test:contracts && npm run test:integration && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
